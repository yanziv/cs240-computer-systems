# This file describes other files that should *not* be
# tracked by git.
# Lines prefixed by # are comments.  Empty lines are ignored.
# Non-comment non-empty lines contain one pattern per line.

# Do not track any file whose name has ~ as a suffix
# or uses the '#' character.
# (Emacs and other editors use such files as backups.)
*~
\#*\#
.\#*

# Ignore build artifacts
*.dSYM
.DS_Store
*.o

# The zero executable
zero
*.bin

# VSCode
.nfs*
.vscode
